type ClinicManagementMember {
    id: ID!
    name: String!
    position: String
    bio: String
    photo: String
    is_active: Boolean!
}

extend type Query {
    # جلب كل الأعضاء
    clinicManagementMembers: [ClinicManagementMember!]!
        @field(
            resolver: "App\\GraphQL\\Resolvers\\ClinicManagementMemberResolver@list"
        )
        @guard(with: ["sanctum"])

    # جلب عضو محدد
    clinicManagementMember(id: ID!): ClinicManagementMember
        @field(
            resolver: "App\\GraphQL\\Resolvers\\ClinicManagementMemberResolver@show"
        )
        @guard(with: ["sanctum"])
}

extend type Mutation {
    # إنشاء عضو
    createClinicManagementMember(
        name: String!
        position: String
        bio: String
        is_active: Boolean
        photo: Upload
    ): ClinicManagementMember
        @field(
            resolver: "App\\GraphQL\\Resolvers\\ClinicManagementMemberResolver@create"
        )
        @guard(with: ["sanctum"])

    # تحديث عضو
    updateClinicManagementMember(
        id: ID!
        name: String
        position: String
        bio: String
        is_active: Boolean
        photo: Upload
    ): ClinicManagementMember
        @field(
            resolver: "App\\GraphQL\\Resolvers\\ClinicManagementMemberResolver@update"
        )
        @guard(with: ["sanctum"])

    # حذف عضو
    deleteClinicManagementMember(id: ID!): Boolean
        @field(
            resolver: "App\\GraphQL\\Resolvers\\ClinicManagementMemberResolver@destroy"
        )
        @guard(with: ["sanctum"])
}


